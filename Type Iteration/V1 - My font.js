function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(0);
  textAlign(CENTER, CENTER);
  textSize(120);
  fill(255);
  noStroke();
  text("Gramatika", width / 2, height / 2);
}
